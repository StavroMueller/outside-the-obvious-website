# Feature Ideas

## "Get this as a print"
Each photo could have a link underneath that says "get this as a print", which then uses the photo url (`street/1.png`) as an ID to pass to a new page where they can quickly order a print in many sizes.

